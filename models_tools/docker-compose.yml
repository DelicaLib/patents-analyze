services:
  jupyter:
    image: jupyter/scipy-notebook
    volumes:
      - ./:/app
    working_dir: /app
    ports:
      - "8888:8888"
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    runtime: nvidia